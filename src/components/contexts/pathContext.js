import React from 'react';

const paths = {
        mainPath : "/web-portfolio",
        resumePath: "/web-portfolio/resume",
        projectPath: "/web-portfolio/project",
        exchangePath: "/web-portfolio/exchange"
    }

export default React.createContext(paths);